// Retry configuration constants
export const DEFAULT_MAX_RETRIES = 3;
export const DEVICE_STATUS_MAX_RETRIES = 2;

// Exponential backoff base delay (milliseconds)
export const RETRY_BASE_DELAY_MS = 1000;
